@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Car", null,
    new { @class = "btn btn-block btn-default btn-lg" })

@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Car",
    action = "List",
    carBrand = link
}, new
{
    @class = "btn btn-block btn-default btn-lg"
})
}